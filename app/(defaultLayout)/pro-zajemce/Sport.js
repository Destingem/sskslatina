"use client"
import SectionHeader from "../../components/Universal/SectionHeader";
export default function Sport({device, id}){
    return(
        <section id={id} style={{maxWidth: "100%"}}>
        <SectionHeader mainText="Sportovní střelba jako sport" subtitle=""></SectionHeader>
       
          

    </section>
    )
}