import SectionHeader from "../../components/Universal/SectionHeader";
export default function Zazemi({device, id}){
    return(
        <section id={id} style={{maxWidth: "100%"}}>
        <SectionHeader mainText="Zázemí" subtitle=""></SectionHeader>
       
          

    </section>
    )
}