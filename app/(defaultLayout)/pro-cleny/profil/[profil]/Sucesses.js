import SectionHeader from "../../../../components/Universal/SectionHeader";
export default function Sucesses({id, device, }){

    return(
        <section id={id} style={{maxWidth: "100%"}}>
            <SectionHeader mainText="Úspěchy" subtitle="Nejlepší umístění střelce v různých disciplínách"></SectionHeader>
           </section>
    )
}